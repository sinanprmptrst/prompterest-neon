# ── Backend ───────────────────────────────────────────────────
# Neon serverless PostgreSQL connection string
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require

# Random string for JWT signing (generate: openssl rand -hex 32)
JWT_SECRET=change_me_to_a_random_secret

# ── Frontend (baked in at Docker build time) ──────────────────
# Primary LLM provider (https://openrouter.ai)
VITE_OPENROUTER_KEY=sk-or-...

# HuggingFace token (image gen + LLM fallback)
VITE_HF_TOKEN=hf_...

# ImgBB API key for permanent image hosting (optional)
VITE_IMGBB_KEY=

# Override default models (optional)
VITE_CHAT_MODEL=
VITE_IMAGE_MODEL=
